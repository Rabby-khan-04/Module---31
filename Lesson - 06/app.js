const add = (first, second) => first + second;

const fullName = (first, last) => first + " " + last;

const multliply = (a, b) => a * b;

console.log(multliply(7, 8));

const getPie = () => Math.PI;

console.log(getPie());

const fiveTime = (num) => num * 5;
